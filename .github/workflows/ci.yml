name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.4', '8.5']
    steps:
      - uses: actions/checkout@v7

      - uses: actions/checkout@v7
        with:
          repository: componenta/app
          ref: main
          path: app-source

      - uses: actions/checkout@v7
        with:
          repository: componenta/di
          ref: main
          path: di-source

      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer:v2

      - run: composer config --working-dir=app-source version 3.0.2
      - run: composer config --working-dir=di-source version 4.0.2
      - run: composer config repositories.app path app-source
      - run: composer config repositories.di path di-source
      - run: composer validate --strict
      - run: composer update --no-interaction --prefer-dist
      - run: composer test
